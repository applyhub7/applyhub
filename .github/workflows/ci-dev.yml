name: CI dev pipleine

on:
  pull_request:
    branches: ["dev-ci"]

jobs:
    test:
        runs-on: ubuntu-latest
        
        steps:
        - uses: actions/checkout@v4
        - name: List files
          run: ls -la
